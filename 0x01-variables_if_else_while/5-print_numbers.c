#include <stdio.h>

/**
 * main - print all single digit of bas
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
